# 更新日志

## 2026-09-19

### Smirel 主页重构

- 参考 LMU 的信息架构重构 Smirel 主页：首屏、痛点、能力、工具兼容、对比、接入步骤、套餐、FAQ 和转化入口。
- 新访客默认使用浅色主题，保留深色与跟随系统偏好。
- 新增公开只读套餐目录接口，主页直接展示后台配置的在售套餐与价格，不公开支付和调度内部字段。
- 支持从主页套餐卡跳转到已选中的充值/订阅套餐。

## 2026-09-12

### 管理员分配用户额度

- 在用户管理页面增加管理员额度调整入口。
- 支持增加额度、扣减额度和设置指定余额，并提供金额校验、调整后余额预览、备注及成功/失败提示。
- 复用 `POST /api/v1/admin/users/:id/balance` 接口，保留原子余额更新、余额非负保护、幂等处理、缓存失效和调整流水记录。
- 增加预览模式下的额度调整模拟，不会修改数据库。

### 部署

- 基于 `release/smirel-commercial-dark` 分支重新构建 `termrelay-commercial-dark:local` 镜像。
- 已通过前端类型检查和生产构建验证。
- 仅重建应用容器，PostgreSQL、Redis 及持久化数据卷均已保留。

### 分支同步与清理

- 按 `release/smirel-commercial-dark` 分支内容对齐实现，并移除不活跃的 CQU 浏览器桥接路由、配置、测试和供应商资源。
- 管理后台增加已有上游账户的编辑功能。
- 模型广场和分组页面仅保留当前启用的商业供应商范围。
- 保留 Apple Container 部署所需的本地构建调整，包括 npm 镜像参数和前端内存限制。

## 2026-09-14

### 支付、退款与余额账本

- 完善支付、订阅、充值和订单管理流程，并补齐管理端支付配置、支付方案及支付渠道页面。
- 新增用户余额账本和余额流水记录，支持余额变更的可追溯查询。
- 完善退款预览、退款计算、退款分账和余额冲回逻辑。
- 新增共建者分成及收入分账能力，支持多受益人固定比例分账和分成审计。
- 新增管理端模拟支付功能，无需真实商户凭据即可验证支付链路。
- 支持无上游退款接口时的线下退款记账流程。

### 数据库迁移

- 新增 `193_smiral_pricing.sql`，补充 Smirel 定价相关结构。
- 新增 `194_user_balance_ledger.sql`，建立用户余额账本。
- 新增 `195_smiral_plans_seed.sql`，初始化 Smirel 套餐数据。
- 新增 `196_revenue_split.sql`，建立收入分账相关结构。

### 同步已部署发布分支

- 核对并记录上游 `release/smirel-commercial-dark` 最近部署提交 `4888d4ead`。
- 该发布线单独同步到 fork 的同名分支；`main` 继续作为本地默认部署版本。
- 未将 release 线回退性删除的支付、退款、余额账本和共建分成实现合并进 `main`。

### 计费订阅页面优化

- 将上游已部署 release 线的订阅卡片视觉优化移植到 `main`。
- 增加套餐品牌、等级、有效期、额度、计费倍率和权益信息展示。
- 增加按量计费及官方 API 折算价格对比、节省比例和立即开通入口。
- 增加计费页面标题区的安全支付提示，并保留 `main` 的支付、余额账本和订单流程。
