# TermRelay 技术治理基线

本文件描述 TermRelay 软件仓库、正式版本和生产基础设施之间的基本边界。它不是商业合作合同，也不改变上游开源代码的许可证和著作权归属。

## 1. 官方软件历史

本仓库 `yuchenm1303-png/termrelay` 作为 TermRelay 项目版本演进、代码变更和正式发布记录的长期入口。

正式上线的软件版本应能够对应到：

- 本仓库中的明确 commit / tag；
- GitHub Release；
- 由本仓库 GitHub Actions 构建的容器镜像及其 digest。

不要把某一台生产服务器上的临时代码视为唯一“正式版本”。

## 2. 生产服务器是运行环境，不是项目唯一归宿

生产服务器可以由合作运营方提供和维护，但服务器上不应存在以下任何“唯一副本”：

- 唯一源码；
- 唯一数据库；
- 唯一部署说明；
- 唯一正式版本记录；
- 唯一灾难恢复资料。

服务器故障或迁移时，应能够从可信代码版本和经授权的数据库备份重新部署。

## 3. 凭据最小权限

生产服务器不应持有：

- GitHub 主账号密码；
- 能删除或转移仓库的管理 Token；
- 不必要的仓库写权限；
- 数据库备份的长期解密私钥。

服务器只获取完成运行和部署所需要的最小权限。

## 4. 数据与备份

业务数据库包含用户、支付、账号和使用记录等敏感数据。备份应：

- 经过项目授权；
- 默认加密；
- 至少有一份位于生产服务器之外；
- 按适用的隐私、数据保护和合作约定保管；
- 定期验证可以恢复。

备份机制不得被改造成秘密导出用户数据的渠道。

## 5. 管理权限

TermRelay 后台长期目标是把日常管理权限和高危所有者操作区分开。

高危操作（例如删除最高权限成员、清空关键数据、改变核心结算配置）应有更强确认和审计，而不是依赖隐藏超级账号或万能 Token。

在 Owner 权限模型完整实现并测试前，不通过临时后门替代它。

## 6. 审计

管理员对用户、余额、订单、账号和关键系统配置的高风险修改，应逐步进入可追溯的审计记录。

审计日志的目的，是让合作运营中出现问题时能够回答“谁、何时、改了什么”，而不是监控正常用户。

## 7. 开源许可证边界

TermRelay 当前仓库包含来自上游 Sub2API 的 LGPL-3.0 授权代码。本治理文件不撤销上游作者已经授予的许可证权利，也不把上游作者的代码声明为 TermRelay 单方面所有。

项目控制权应建立在 TermRelay 自己的持续开发、正式发布历史、品牌运营、授权业务数据、部署恢复能力和后续原创代码上，而不是违反开源许可证限制他人正常使用已有开源代码。

## 8. 明确禁止的控制方式

不使用以下方式建立合作中的“主动权”：

- 隐藏远程停服开关；
- 到期自毁代码；
- 远程删库；
- 秘密管理员账号；
- 绕过权限系统的万能密钥；
- 未经授权的数据外传；
- 将合作方正常拥有的服务器权限偷偷转化为第三方无法发现的持续访问。

TermRelay 的技术主动权来自：**可信版本 + 独立恢复 + 最小权限 + 审计 + 明确治理**。
